# Ensure consistent dependency installation
save-exact=true
package-lock=true

# Security
audit-level=moderate
min-release-age=3
allow-git=root

# Turborepo remote caching (optional, requires setup)
# @turbo:registry=https://registry.turbo.build
