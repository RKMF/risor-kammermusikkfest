# ============================================
# Site Configuration
# ============================================
PUBLIC_SITE_URL=http://localhost:4321

# ============================================
# Sanity CMS Configuration
# ============================================

# Sanity Project ID (Required)
# Find this in your Sanity project settings at manage.sanity.io
PUBLIC_SANITY_PROJECT_ID=dnk98dp0

# Sanity Dataset (Required)
# Use "production" for live data, "development" for testing
PUBLIC_SANITY_DATASET=production

# Sanity API Version (Required)
# Use latest stable version for new features
# Format: YYYY-MM-DD
PUBLIC_SANITY_API_VERSION=2025-01-01

# Sanity API Read Token (Required for data fetching)
# Generate at: manage.sanity.io → API → Tokens → Add API token
# Permissions needed: Viewer (read-only access)
# IMPORTANT: Keep this secret! Never commit the actual token to git
SANITY_API_READ_TOKEN=your_read_token_here

# ============================================
# Visual Editing Configuration (Development)
# ============================================

# Enable Visual Editing mode (true/false)
# When enabled, allows real-time preview of content changes from Sanity Studio
PUBLIC_SANITY_VISUAL_EDITING_ENABLED=true

# Sanity Studio URL (for Visual Editing)
# Local development: http://localhost:3333
# Production: https://your-studio-domain.com
PUBLIC_SANITY_STUDIO_URL=http://localhost:3333

# ============================================
# Environment Configuration
# ============================================

# Node environment (development/production/test)
NODE_ENV=development

# Site environment for staging-only content (staging/production)
# On staging: shows content with publishingStatus "staging" or "published"
# On production: shows only content with publishingStatus "published"
# Set in Vercel: "staging" for testing.kammermusikkfest.no, "production" for www
PUBLIC_SITE_ENV=production

# ============================================
# Optional: Security Configuration
# ============================================

# Content Security Policy report endpoint (optional)
# Uncomment if you want to receive CSP violation reports
# CSP_REPORT_URI=https://your-csp-report-endpoint.com/report

# ============================================
# Optional: Analytics (Future)
# ============================================

# Google Analytics Tracking ID (if using analytics)
# PUBLIC_GA_TRACKING_ID=

# ============================================
# Setup Instructions
# ============================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Replace "your_read_token_here" with your actual Sanity API token
#    - Go to https://manage.sanity.io
#    - Select your project
#    - Go to API → Tokens
#    - Create a new token with "Viewer" permissions
#
# 3. Update PUBLIC_SITE_URL if deploying to production
#
# 4. For Visual Editing to work, you need:
#    - Both servers running (studio on 3333, frontend on 4321)
#    - PUBLIC_SANITY_VISUAL_EDITING_ENABLED=true
#    - Valid SANITY_API_READ_TOKEN
#    - Preview mode cookie set (happens automatically in Visual Editing)
#
# 5. NEVER commit your .env file to git!
#    - It's already in .gitignore
#    - Only commit this .env.example file
#
